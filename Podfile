platform :ios, '13.0'  # 设置最低支持的 iOS 版本

target 'CHPictureEditTools' do
  use_frameworks!         
  pod "ZLPhotoBrowser" #, "4.5.8"
  pod 'SnapKit'
  
  target 'Example' do
    inherit! :complete
  end
  
  target 'ExampleTests' do
    inherit! :complete
  end
  
  target 'ExampleUITests' do
    inherit! :complete
  end
  
end
